import 'package:flutter/material.dart';

final kStoryGradientBorder = BoxDecoration(
  gradient: LinearGradient(
      colors: [Colors.yellow.shade600, Colors.orange, Colors.red]),
  shape: BoxShape.circle,
);
const kStoryBlackBorder = BoxDecoration(
  color: Colors.black,
  shape: BoxShape.circle,
);

const kCircleBackground = BoxDecoration(
  color: Colors.grey,
  shape: BoxShape.circle,
);
